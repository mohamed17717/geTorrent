mixin link(anchor)
  a(href=anchor.href, target="_blank")= anchor.text

mixin youtube(src)
  iframe(width="560", height="315", src=src, frameborder="0", allow="accelerometer; autoplay; encrypted-media; gyroscope; picture-in-picture", allowfullscreen)
